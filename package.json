{
  "name": "my_schedules",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "create-db": "psql -U postgres -f sql/create_db.sql",
    "create-table": "psql -U postgres -d my_schedules -f sql/create_table.sql",
    "seed-table": "psql -U postgres -d my_schedules -f sql/seed_table.sql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "pg-promise": "^10.11.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
